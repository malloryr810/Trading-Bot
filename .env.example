# Optional API keys for alternative data providers (yfinance needs none).
# See docs/data_sources.md. Leave blank unless a provider is wired up.
MARKET_DATA_API_KEY=
NEWS_API_KEY=

# Runtime environment: "development" or "production".
ENVIRONMENT=development

# SQLite database file path (see app/config.py).
DATABASE_PATH=data/investment_bot.db
